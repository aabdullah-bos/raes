# RAES Execute

## Durable Decisions

| Decision | Rationale | Date |
| -------- | --------- | ---- |
| CLI tool enforces one slice per invocation; no multi-slice batch execution | PRD explicitly states "Does not support automated multi-slice/multi-milestone batch execution (one slice per invocation)." This boundary prevents drift and ensures disciplined, step-by-step advancement with human oversight at each decision point. | 2026-05-03 |
| Artifact files must never be merged, overwritten, or reinterpreted outside strict RAES rules                                                                                                                                                                                                                                                                                                    PRD states "Does not merge, overwrite, or reinterpret artifact files outside the strict rules defined in RAES." This is non-negotiable for maintaining artifact integrity and trust in system constraints, decisions, and product intent.                                                                                                                                                                                                                                                                                                   | 2026-05-03 |
| CLI halts execution if artifact boundary is violated or unknown artifact is referenced in config                                                                                                                                                                                                                                                                                                                                                                                                                                                     | PRD Safety/Drift Guards (Priority: High) requires "Disallow execution if artifact boundary is violated or unknown artifact is referenced in config." Prevents silent corruption and forces human resolution of ambiguity before proceeding.                                                                                                                                                                                                                                                                                                               | 2026-05-03 |
| Durable constraints promoted from decisions.md must be referenced in system.md (and vice versa); tool must validate or warn when boundary is unclear                                                                                                                                                                                                                                                                                                                                                                                                 | PRD Boundary/Artifact Discipline requirement ensures constraints and rationale never mingle and "truth lives" in the correct artifact. Validation at execution time prevents drift.                                                                                                                                                                                                                                                                                                                                                                       | 2026-05-03 |
| CLI is workflow enforcer, not code generator or agent                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | PRD Non-Goals state "Not a code generator, agent, or prompt optimizer — enforces workflow, not content creativity." CLI responsibility is guardrails and boundaries, not creative automation.                                                                                                                                                                                                                                                                                                                                                             | 2026-05-03 |
| All artifact reads and writes must be local to configured project root; no remote calls or touching non-RAES files                                                                                                                                                                                                                                                                                                                                                                                                                                   | PRD Data Storage & Privacy section: "Reads and writes only to explicitly configured files in the current project root (never touches non-RAES files)" and "All user and slice history data stored locally; no remote calls." Ensures safety, privacy, and clear file ownership.                                                                                                                                                                                                                                                                           | 2026-05-03 |
| CLI core commands (excluding --history) must execute in under 200ms on commodity hardware                                                                                                                                                                                                                                                                                                                                                                                                                                                            | PRD Scalability & Performance: "CLI should execute core commands (other than --history) in under 200ms on commodity hardware." Ensures responsiveness and practical usability in daily workflow.                                                                                                                                                                                                                                                                                                                                                          | 2026-05-03 |
| raes.config.yaml must exist and be valid before any execution; config validation is a mandatory pre-flight check                                                                                                                                                                                                                                                                                                                                                                                                                                     | PRD Functional Requirement --check-config and Safety/Drift Guards ensure artifact paths and types are validated before execution begins. Prevents downstream errors and guides self-healing.                                                                                                                                                                                                                                                                                                                                                              | 2026-05-03 |
| History and log output must be machine-readable to support future ingestion and analysis                                                                                                                                                                                                                                                                                                                                                                                                                                                             | PRD Extensibility/Auditability requirement: "History/log output and flagging designed to be machine-readable for later ingestion/analysis." Enables audit trail and integration with downstream tooling.                                                                                                                                                                                                                                                                                                                                                  | 2026-05-03 |
| Flagging mechanism must halt advancement until ambiguity is resolved and flag is cleared by human                                                                                                                                                                                                                                                                                                                                                                                                                                                    | PRD User Experience (Advanced Features): "User flags ambiguity: Tool halts advancement until issue is resolved and flag is cleared, ensuring no action 'papers over' uncertainty." Prevents technical debt from buried ambiguity.                                                                                                                                                                                                                                                                                                                         | 2026-05-03 |
| CLI outputs must be color-coded with clear icons/symbols; error messages must state what is blocking, why, and what file/data needs fixing                                                                                                                                                                                                                                                                                                                                                                                                           | PRD UI/UX Highlights require "Strict, concise error messages: always say what is blocking, why, and what file or data needs to be fixed before proceeding." Supports operator clarity and reduces support burden.                                                                                                                                                                                                                                                                                                                                         | 2026-05-03 |
| Artifact file updates must be atomic; never partial writes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | PRD Technical Needs: "Safe, atomic file I/O for updating artifacts; never partial writes." Ensures data integrity and prevents corruption during interruption or failure.                                                                                                                                                                                                                                                                                                                                                                                 | 2026-05-03 |
| Slice execution must walk through either Execution Loop or Review Loop as determined by slice type, enforcing boundaries for each artifact update                                                                                                                                                                                                                                                                                                                                                                                                    | PRD Core Experience Step 2: "Tool walks user/agent through either Execution Loop or Review Loop (prompting, recording, and enforcing boundaries)." Each slice type has distinct rules; CLI enforces them strictly.                                                                                                                                                                                                                                                                                                                                        | 2026-05-03 |
| CLI must validate that all artifact paths referenced in raes.config.yaml are present and of correct type before execution                                                                                                                                                                                                                                                                                                                                                                                                                            | PRD Functional Requirement --check-config (Priority: High) ensures missing or misconfigured artifacts are caught early, preventing silent failures during execution.                                                                                                                                                                                                                                                                                                                                                                                      | 2026-05-03 |
| Core RAES execution flow and artifact rules cannot be bypassed, even via subcommands or extensibility                                                                                                                                                                                                                                                                                                                                                                                                                                                | PRD Extensibility/Auditability: "CLI extensible via subcommands, but core RAES execution flow and artifact rules cannot be bypassed." Protects methodology integrity as tooling evolves.                                                                                                                                                                                                                                                                                                                                                                  | 2026-05-03 |
| Exit codes: 0 = success/help/version; 1 = usage error (unknown option, extra arg, missing required arg); 2 = reserved for config/runtime errors                                                                                                                                                                                                                                                                                                                                                                                                      | system.md contract: "Exit code assignments must be recorded in decisions.md when introduced." These codes are a public contract and must not change without an explicit decision entry.                                                                                                                                                                                                                                                                                                                                                                   | 2026-05-03 |
| Implementation language and runtime: TypeScript with Node.js, `--experimental-strip-types`, no build step, no external runtime dependencies                                                                                                                                                                                                                                                                                                                                                                                                          | Matches the raes-init sibling project exactly; keeps toolchain consistent across the RAES CLI suite and avoids adding a separate compile step.                                                                                                                                                                                                                                                                                                                                                                                                            | 2026-05-03 |
| `main(argv, io)` is the testable entry point; `io.out`/`io.err` are injectable sinks; the binary entry point at file bottom calls `main` and exits with the returned code                                                                                                                                                                                                                                                                                                                                                                            | Decouples command logic from process I/O so tests can assert on output without spawning subprocesses. This pattern must be preserved for all future command handlers.                                                                                                                                                                                                                                                                                                                                                                                     | 2026-05-03 |
| `checkConfig` returns `{ errors: ConfigError[]; config?: RaesConfig }`, not a bare `ConfigError[]`                                                                                                                                                                                                                                                                                                                                                                                                                                                   | On success the caller needs the parsed config to display verified paths and to support future commands (e.g. `--status`, `--list-slices`) that must load config without re-validating from scratch. Returning a bare array forced callers to re-parse the config themselves, duplicating I/O and validation logic. Any future function that validates and then acts on config should follow the same pattern: return errors alongside the parsed result so the caller never has to re-parse on success.                                                   | 2026-05-03 |
| Every `ConfigError` must include a non-empty `fix` string describing the one action that resolves the error                                                                                                                                                                                                                                                                                                                                                                                                                                          | Bare error messages tell the operator what is wrong but not what to do. The PRD requires "strict, concise error messages: always say what is blocking, why, and what file or data needs to be fixed before proceeding." `fix` is the "what to do" half of that contract. Future slices that introduce new validation errors in `extractConfig`, `validatePaths`, or any new validator must populate `fix`; a `ConfigError` without `fix` is incomplete and must not be merged.                                                                            | 2026-05-03 |
| `--status` reads the pipeline file at runtime via `config.sources.next_slice.path`; no caching between invocations                                                                                                                                                                                                                                                                                                                                                                                                                                   | Config is read once at startup per system.md contract. The pipeline file is read on demand only when `--status` is invoked; it is not cached as a side effect of config loading. This keeps the I/O boundary clear and ensures `--status` always reflects the latest pipeline state.                                                                                                                                                                                                                                                                      | 2026-05-03 |
| Milestone is not displayed in v1 `--status` output                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | The pipeline format carries no per-slice milestone metadata; deriving milestone from slice position would be inference, which violates the "no guessing" anti-pattern in execution-guidance. Milestone display is deferred until milestone grouping is added to config or pipeline format. Future slices that add milestone data should extend `Slice` and `PipelineStatus` in `src/pipeline.ts`.                                                                                                                                                         | 2026-05-03 |
| `--list-slices` omits `type` and `assignee` columns in v1                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | The PRD requires these fields, but the current pipeline backlog format (`- [x] Slice N: label`) carries no type or assignee metadata. Displaying them would require inference from label text, violating the no-guessing anti-pattern. Both fields are deferred until the pipeline format explicitly encodes them. Future slices that add this metadata must extend `Slice` in `src/pipeline.ts` and update `formatSliceList`.                                                                                                                            | 2026-05-05 |
| `--show-next-slice` omits `type`, `constraints`, `goal`, `acceptance criteria`, and `related files` in v1                                                                                                                                                                                                                                                                                                                                                                                                                                            | The PRD requires these fields, but the current pipeline backlog format (`- [x] Slice N: label`) carries no metadata beyond position, label, and completion status. Displaying missing fields would require inference from label text, violating the no-guessing anti-pattern. All fields beyond what the format encodes are deferred. Future slices that add structured metadata to the pipeline format must extend `Slice` in `src/pipeline.ts` and update `formatNextSlice`.                                                                            | 2026-05-05 |
| Atomic artifact writes use temp-file + rename: `writeFileSync` to `.raes-tmp-<hex>` in the same directory as the target, then `renameSync` to the final path; on any failure the temp file is removed and an error is returned                                                                                                                                                                                                                                                                                                                       | `rename` is atomic at the OS level when source and target are on the same filesystem (guaranteed by placing the temp file in the same directory). This is the only pattern that prevents the target file from ever being in a partial state. All future artifact write operations in `src/` must use `writeFileAtomic` from `src/io.ts` rather than calling `writeFileSync` on the target path directly.                                                                                                                                                  | 2026-05-05 |
| Loop type for `--execute-next-slice` is inferred from slice label using `/\breview\b/i`; slices whose label contains the whole word "review" (case-insensitive) route to the Review Loop; all others route to the Execution Loop                                                                                                                                                                                                                                                                                                                     | The current pipeline backlog format (`- [x] Slice N: label`) carries no explicit type metadata. Inferring from the label is an acknowledged imprecision but is the minimum viable routing rule for the skeleton. When the pipeline format gains an explicit type field, `determineLoopType` in `src/pipeline.ts` must be updated to read it directly and the inference rule retired.                                                                                                                                                                      | 2026-05-05 |
| CLI calls AI provider directly for execution and review slices; the canonical prompt is submitted unmodified; operator reviews agent output before any artifact is written                                                                                                                                                                                                                                                                                                                                                                           | The CLI is the submission mechanism, not the content author. Invariant 6 ("does not author content") applies to artifact authoring only. The REVIEW gate between provider response and artifact write is operator-gated, not automatic                                                                                                                                                                                                                                                                                                                    | 2026-05-05 |
| Canonical prompt lives in raes-reference.md as the human-readable source of truth; a copy is extracted into src/prompts/execution-loop.md as the runtime source the CLI reads via loadPrompt() in src/prompt.ts; divergence between the two copies is accepted and managed manually                                                                                                                                                                                                                                                                  | Single source of truth is preferred but deferred; the raes-execute copy is the operative one for the CLI; when the canonical prompt changes, the raes-execute copy must be updated explicitly                                                                                                                                                                                                                                                                                                                                                             | 2026-05-05 |
| Provider subprocess file write access is enabled by default; configurable per-provider in raes.config.yaml under provider.sandbox. For Claude Code the default flag is `--allowedTools Edit,Write,Read`; for Codex the default flag is `--sandbox workspace-write`. Operators can override by setting provider.sandbox to a different value in raes.config.yaml; an explicit empty string disables write access. This is recorded here because it affects the public config contract and the subprocess invocation in both provider implementations. | Write access is required for the agent to update pipeline.md and decisions.md as part of the RAES loop. Disabling it silently would cause the agent to complete work but fail to record it, which violates the RECORD step. Default-on with explicit opt-out is safer than default-off with silent failure.                                                                                                                                                                                                                                               | 2026-05-05 |
| Neither ClaudeCodeProvider nor CodexProvider reads or injects API keys; both CLIs authenticate via the operator's pre-existing session (claude login or codex login); the subprocess inherits the operator's environment as-is; auth failure is detected from subprocess non-zero exit code and stderr content, not from key absence                                                                                                                                                                                                                 | Claude Code uses OAuth credentials stored in ~/.claude/.credentials.json or ANTHROPIC_API_KEY if already present in the environment; Codex uses credentials from codex login or silently switches to OPENAI_API_KEY if present. raes-execute injecting keys would override operator authchoices and introduce credential-handling responsibility the CLIshould not own.                                                                                                                                                                                   | 2026-05-05 |
| `raes-execute` targets exactly one RAES project per invocation. By default it reads `raes.config.yaml` from the current working directory; in monorepos it may also support an explicit `--config <path>` override, but it must never do implicit upward or repo-wide discovery. The canonical default config location is the RAES project root, not `docs/`.                                                                                                                                                                                        | Monorepos can contain multiple independent RAES projects, so implicit discovery creates ambiguity and surprising behavior. Current-directory default preserves locality for the common case, while an explicit config override supports multi-project repos without weakening project boundaries.                                                                                                                                                                                                                                                         | 2026-05-05 |
| Shared artifact definitions and cross-tool contracts live in `docs/raes-reference.md`; `raes-execute` local docs adopt those rules rather than redefining them                                                                                                                                                                                                                                                                                                                                                                                       | This keeps `decisions.md` scoped to local rationale and tradeoffs, `system.md` scoped to active execution constraints, and `prd.md` scoped to product requirements. Without that boundary, local docs become competing specs and operators lose a single source of truth for RAES methodology.                                                                                                                                                                                                                                                            | 2026-05-05 |
| `pipeline.md` is scoped to backlog and handoff state only; non-pipeline sections are staged out before redistribution to their owning artifacts                                                                                                                                                                                                                                                                                                                                                                                                      | RAES reference Section 3a permits only `## Slice Backlog` and `## Handoff Notes` in `pipeline.md`. Slice B1 removed `Purpose`, `Invariants`, `Known Contracts`, and `Unknowns` from `pipeline.md` and staged them in `docs/b1-displaced-content.md` for B2/B3 to consume without losing content provenance.                                                                                                                                                                                                                                               | 2026-05-06 |
| Slice B2 reconstructed `system.md` by merging the staged `Invariants`, `Known Contracts`, and `Unknowns` into the existing `system.md` sections, keeping the more accurate live-project wording where the staged copy was stale                                                                                                                                                                                                                                                                                                                      | The staged content correctly identified what belonged in `system.md`, but parts of it still described an older project shape (for example, slice metadata living in config). B2 kept the durable constraints, strengthened them with the staged wording where it was more specific, and avoided reintroducing contracts the current project no longer honors.                                                                                                                                                                                             | 2026-05-06 |
| Slice B2 discarded staged `Known Contracts` claims that RAES Execute stores slice definitions in config and already produces dedicated local status/history files                                                                                                                                                                                                                                                                                                                                                                                    | Those claims no longer match the current project: slice order and completion live in the configured pipeline artifact, and no separate state/history artifact has been introduced yet. Keeping stale contracts in `system.md` would create drift between the docs and the implemented CLI.                                                                                                                                                                                                                                                                | 2026-05-06 |
| `system.md` no longer tracks "Exact configuration schema and required vs optional keys" as an open Unknown                                                                                                                                                                                                                                                                                                                                                                                                                                           | That Unknown was resolved by the implemented config contract in `src/config.ts` and the recorded provider/config decisions from earlier slices. Leaving it open in `system.md` would misstate the current project state.                                                                                                                                                                                                                                                                                                                                  | 2026-05-06 |
| `## Invariants` removed from `execution-guidance.md`; its six constraints are already captured in `system.md` (`## Product Invariants`, `## Drift Guards`, `## Known Contracts`)                                                                                                                                                                                                                                                                                                                                                                     | `raes-reference.md` Section 3a lists `## Invariants` as a forbidden heading in `execution-guidance.md`. The substance of all six invariants was verified present in `system.md` before removal. Invariant 6 ("No AI Creativity or Content Generation") was not yet explicit in `system.md` as a Product Invariant or Drift Guard — flagged in B3 handoff notes for a follow-up human promotion.                                                                                                                                                           | 2026-05-06 |
| `## Definition of Done` removed from `system.md`; consolidated into `execution-guidance.md`                                                                                                                                                                                                                                                                                                                                                                                                                                                          | `execution-guidance.md` already contained a more comprehensive seven-item Definition of Done covering artifact boundaries, ambiguity, history, acceptance criteria, config validity, and operator readiness. The `system.md` DoD contained process rules (failing tests first, minimum implementation) that are captured by the canonical RAES execution loop sequence in `raes-reference.md` Section 4 and by the Workflow Rules in `execution-guidance.md`. Duplication removed; `execution-guidance.md` is now the sole DoD location for this project. | 2026-05-06 |
| `execution-guidance.md` Workflow Rules 3 and 4 now contain explicit loop sequences instead of circular references                                                                                                                                                                                                                                                                                                                                                                                                                                    | Rules 3 and 4 previously said "according to RAES execution loop rules" without stating the sequences. The explicit sequences (PLAN → SLICE → EXECUTE → TEST → EXPLAIN → FLAG → REVIEW → RECORD and PLAN → SLICE → INSPECT → SYNTHESIZE → FLAG → REVIEW → RECORD) are now inlined, matching `raes-reference.md` Section 4. Rules also updated to reflect that the CLI submits via provider, not prompts the operator interactively.                                                                                                                        | 2026-05-06 |
| `execution-guidance.md` Milestone 1 toolchain reference replaced; performance target corrected to 200ms                                                                                                                                                                                                                                                                                                                                                                                                                                              | The CLI library options list (Click, Commander.js, Cobra) was removed and replaced with a pointer to the toolchain decision already recorded in `decisions.md`. The `<100ms` performance baseline was corrected to `<200ms` to match the recorded contract.                                                                                                                                                                                                                                                                                               | 2026-05-06 |
| `docs/b1-displaced-content.md` deleted; remaining `## Purpose` staged section treated as absorbed by `prd.md` existing TL;DR                                                                                                                                                                                                                                                                                                                                                                                                                         | The staged `## Purpose` content is equivalent to the existing `### TL;DR` section in `prd.md`. The heading `## Purpose` is not a permitted heading in `prd.md` per `raes-reference.md` Section 3a; the content is already represented. No separate insertion needed. The staging file is deleted with all content accounted for.                                                                                                                                                                                                                          | 2026-05-06 |
| Prefer Codex app-server over `codex exec --json` for rich operator-visible progress in `raes-execute` | The current `codex exec --json` integration is sufficient for one-shot execution but is a weak fit for RAES operator feedback because its event stream is thinner and less clearly documented for subprocess integration. The Codex app-server protocol is the authoritative reference for long-lived turn lifecycle events, item lifecycle events, streamed deltas, command execution updates, plan updates, and diff updates. Future work to improve execution/review slice observability should treat the Codex app-server protocol as the primary OpenAI integration path and use `codex exec` only as a fallback or compatibility path during rollout. Reference documents: OpenAI Codex repository `docs/exec.md`; OpenAI Codex repository `codex-rs/app-server/README.md`; OpenAI article “Unrolling the Codex agent loop”. | 2026-05-06 |
